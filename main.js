import {customAlert, customConfirm, customPrompt} from './customdialog.js';

/*create object */
var alertObject = new customAlert();
var confirmObject = new customConfirm(); 
var promptObject = new customPrompt();